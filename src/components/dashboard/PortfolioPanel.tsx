import { BarChart3, CircleDollarSign, PieChart, TrendingUp } from "lucide-react";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { useLanguage } from "@/lib/i18n";
type Portfolio={balance:number;invested:number;performance_pct:number;today_pnl:number;total_deposited:number;total_pnl:number};
function money(value:number){return new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(value)}
export function PortfolioPanel({portfolio,hasPortfolio=true}:{portfolio:Portfolio;hasPortfolio?:boolean}){const {language}=useLanguage();const en=language==="en";const m=en?{title:"Portfolio",desc:"Consolidated view of your capital and results.",current:"Current equity",invested:"Invested capital",pnl:"Accumulated P&L",perf:"Performance",today:"Today's result",todayDesc:"P&L recorded in the portfolio.",deposited:"Total deposited",depDesc:"Capital added to the account.",history:"Performance history",historyDesc:"The historical chart will be enabled when a performance time series exists in Supabase. No historical data is invented.",emptyTitle:"No portfolio yet",emptyDesc:"There is no portfolio record for this account yet. Financial values remain at zero until real account data exists."}:{title:"Portafolio",desc:"Vista consolidada de tu capital y resultados.",current:"Patrimonio actual",invested:"Capital invertido",pnl:"P&L acumulado",perf:"Rendimiento",today:"Resultado de hoy",todayDesc:"P&L registrado en el portafolio.",deposited:"Total depositado",depDesc:"Capital ingresado a la cuenta.",history:"Histórico de rendimiento",historyDesc:"El gráfico histórico se habilitará cuando exista una serie temporal de rendimiento en Supabase. No se inventan datos históricos.",emptyTitle:"Aún no hay portafolio",emptyDesc:"Todavía no existe un registro de portafolio para esta cuenta. Los valores financieros permanecen en cero hasta que existan datos reales."};const metrics=[{label:m.current,value:money(portfolio.balance),icon:CircleDollarSign},{label:m.invested,value:money(portfolio.invested),icon:PieChart},{label:m.pnl,value:money(portfolio.total_pnl),icon:TrendingUp},{label:m.perf,value:`${portfolio.performance_pct>=0?"+":""}${portfolio.performance_pct.toFixed(2)}%`,icon:BarChart3}];return <Card id="portafolio" className="scroll-mt-24 border-slate-200 shadow-sm"><CardHeader><CardTitle>{m.title}</CardTitle><p className="text-sm text-slate-500">{m.desc}</p></CardHeader><CardContent className="space-y-5">{!hasPortfolio&&<div className="rounded-xl border border-dashed border-amber-300 bg-amber-50/60 p-4"><p className="font-medium text-slate-900">{m.emptyTitle}</p><p className="mt-1 text-sm text-slate-600">{m.emptyDesc}</p></div>}<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({label,value,icon:Icon})=><div key={label} className="rounded-xl bg-slate-50 p-4"><Icon className="h-5 w-5 text-blue-600"/><p className="mt-3 text-xs text-slate-500">{label}</p><p className="mt-1 text-lg font-bold text-slate-900">{value}</p></div>)}</div><div className="grid gap-4 md:grid-cols-2"><div className="rounded-xl border border-slate-200 p-4"><p className="text-sm font-medium">{m.today}</p><p className={`mt-2 text-2xl font-bold ${portfolio.today_pnl>=0?"text-emerald-600":"text-red-600"}`}>{portfolio.today_pnl>=0?"+":""}{money(portfolio.today_pnl)}</p><p className="mt-1 text-xs text-slate-500">{m.todayDesc}</p></div><div className="rounded-xl border border-slate-200 p-4"><p className="text-sm font-medium">{m.deposited}</p><p className="mt-2 text-2xl font-bold text-slate-900">{money(portfolio.total_deposited)}</p><p className="mt-1 text-xs text-slate-500">{m.depDesc}</p></div></div><div className="rounded-xl border border-dashed border-slate-300 bg-slate-50/70 p-5"><p className="font-medium">{m.history}</p><p className="mt-1 text-sm text-slate-500">{m.historyDesc}</p></div></CardContent></Card>}
